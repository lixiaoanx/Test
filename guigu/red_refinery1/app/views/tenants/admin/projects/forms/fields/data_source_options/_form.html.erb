<%= form_with(model: options, scope: :options, url: tenant_admin_project_form_field_data_source_options_path(tenant, project, form, field), method: :patch, local: true) do |f| %>
  <%= render "_form_core/data_source_options/#{options.type_key}", f: f, options: options %>

  <%= f.submit t("shared.form.submit.update"), class: "btn btn-block btn-primary" %>
<% end %>
