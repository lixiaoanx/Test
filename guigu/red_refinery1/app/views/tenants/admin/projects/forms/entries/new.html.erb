<%#- add_to_breadcrumbs t(".title") %>

<div class="row justify-content-center">
  <div class="col-lg-12">
    <%= render "tenants/admin/projects/forms/tabs" %>

    <div class="card">
      <div class="card-body">
        <%= render "_form_core/render/form",
                   form: @form, instance: @record,
                   options: {scope: :record, url: tenant_admin_project_form_entries_path(@tenant, @project, @form), local: true} %>
      </div>
    </div>
  </div>
</div>
