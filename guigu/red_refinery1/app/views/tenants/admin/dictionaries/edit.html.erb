<%- content_for :action_bar do %>
  <ol>
    <li class="action_bar-item">
      <%= link_to t("shared.actions.back"), tenant_admin_dictionaries_path(@tenant), class: "btn text-primary" %>
    </li>
  </ol>
<% end %>

<div class="row justify-content-center">
  <div class="col-lg-12">
    <div class="card tab-content">
      <div class="card-body">
        <%= render 'form', tenant: @tenant, dictionary: @dictionary %>
      </div>
    </div>
  </div>
</div>
